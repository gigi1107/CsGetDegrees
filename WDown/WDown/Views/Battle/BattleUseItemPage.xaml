<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WDown.Views.Battle.BattleUseItemPage">
    <ContentPage.Content>
        <ScrollView>
            <!--<StackLayout>
                <Label Text=" Welcome to your Inventory!"
                       FontSize="Large"
                       HorizontalOptions="CenterAndExpand"></Label>
            </StackLayout>-->
            <StackLayout>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <!--<RowDefinition Height="*" />-->
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <!-- Character photo & stats-->
                    <Image x:Name="CharacterImage" Source="{Binding pickedCharacter.Data.ImageURI}" Grid.Row="0" Grid.Column="0" HeightRequest="50" WidthRequest="50" HorizontalOptions="Center"/>

                    <Grid Grid.Row="0" Grid.Column="1" HorizontalOptions="Start">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <!--<RowDefinition Height="*" />-->
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackLayout Grid.Row="0" Grid.Column="0">
                            <Label Text="Attack" FontSize="Medium"></Label>
                            <Label x:Name="CharacterAttack" Text="{Binding pickedCharacter.Data.Attribute.Attack}"></Label>
                        </StackLayout>

                        <StackLayout Grid.Row="0" Grid.Column="1" >
                            <Label Text="Defense" FontSize="Medium"></Label>
                            <Label x:Name="CharacterDefense" Text="{Binding pickedCharacter.Data.Attribute.Defense}"></Label>
                        </StackLayout>

                        <StackLayout Grid.Row="0" Grid.Column="2" >
                            <Label Text="Speed" FontSize="Medium"></Label>
                            <Label x:Name="CharacterSpeed" Text="{Binding pickedCharacter.Data.Attribute.Speed}"></Label>
                        </StackLayout>

                        <StackLayout Grid.Row="0" Grid.Column="3" >
                            <Label Text="HP" FontSize="Medium"></Label>
                            <Label x:Name="CharacterCurrentHealth" Text="{Binding pickedCharacter.Data.Attribute.CurrentHealth}"></Label>
                        </StackLayout>

                        <StackLayout Grid.Row="0" Grid.Column="4" >
                            <Label Text="Name" FontSize="Medium"></Label>
                            <Label x:Name="CharacterName" Text="{Binding pickedCharacter.Data.ExperienceTotal}"></Label>
                        </StackLayout>
                    </Grid>
                </Grid>

                <!-- item photo -->
                <Image x:Name="ItemImage" Source="http://www.pngmart.com/files/4/Green-Light-PNG-Photos.png" WidthRequest="200" HeightRequest="200"/>

                <!-- item info -->
                <StackLayout>
                    <Label x:Name="ItemDescription" Text="{Binding }" />
                    <Label x:Name="ItemAffectsLabel" Text="{Binding }" />
                </StackLayout>

                <!-- item picker 
                <Picker x:Name="ItemSelectPicker" Title="Item Select" ItemsSource="{Binding AvailableItems}"/>-->

                <ListView x:Name="AvailableItemListView"
                          ItemsSource="{Binding AvailableItems}"
                          VerticalOptions="FillAndExpand"
                          HasUnevenRows="true"
                          RefreshCommand="{Binding LoadItemsCommand}"
                          IsPullToRefreshEnabled="true"
                          IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                          CachingStrategy="RecycleElement"
                          ItemSelected="OnItemSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Label Text="{Binding Name}" />
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>


                <Button Text="Save" Clicked="SaveButtonClicked" />
                <Button Text="Cancel" Clicked="CancelButtonClicked" />
                <!-- item info
                <StackLayout>
                    <Label x:Name="ItemDescLabel" Text="{Binding }" />
                    <Label x:Name="ItemAffectsLabel" Text="{Binding }" />
                </StackLayout>-->

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>